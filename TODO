问题：
发生器产生128k的数据，通过某个描述符发送给服务器，epoll检测到某个fd有in,把这个任务分给一个线程去处理
把这128k每1k的读到缓冲区。并使用epoll_out写出。
接收到服务器的数据，读入并保存。

头部定义
标识头部(2字节)|数据长度(ll,hh)|时间戳(us)|[数据]

发生器大改：
随机挑选合适的fd发送
使用epoll_out发送数据

接收到的数据怎么占位(空洞)


随机准备发送任务，设置为pullout,
发生器刚开始设置为epoll_out 发送数据之后设置为epoll_in


缓冲的类
使用循环缓冲，每个缓冲给2k的量